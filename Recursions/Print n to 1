#include <bits/stdc++.h>
#define ll long long
using namespace std;


int cnt=1;

void solve(int i, int n){
if(i>n)return;
solve(i+1, n);
cout<<i<<endl;
}

int main() {
    int n;
    cin>>n;
    solve(1, n);
	return 0;
}
